// @ts-nocheck
import React from 'react';

/**
 * Minimal AppProvider - Simplified for initial testing
 * All complex providers commented out to focus on navigation and UI
 */
export function AppProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
